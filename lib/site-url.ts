/**
 * Single source of truth for the site's canonical base URL.
 * Used for metadata (canonical, OG, Twitter), sitemap, robots, and RSS.
 * Must be https only — no http, no www — for Google Search Console and recrawl.
 */
export const SITE_URL = 'https://healthnutritionhacks.com';
